# Netlify 部署設定 — 神木防衛 (Vite)
[build]
  command = "npm run build"
  publish = "dist"

# SPA / 前端路由：全部回 index.html（目前無多頁也安全）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
